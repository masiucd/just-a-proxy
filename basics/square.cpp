#include <iostream>

#include <cmath>

int main()
{
  double x, y, z = 0;
  x = 2;
  y = 16;
  z = 4;

  std::cout << "Roten ur y = " << sqrt(y) << std::endl;
  std::cout << " Upphojt z och x = " << pow(z, x) << std::endl;

  system("PAUSE");
  return 0;
}